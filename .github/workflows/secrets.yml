name: Secret Demo
on:
  workflow_dispatch:

permissions:
  contents: read  # least privilege example

jobs:
  show-secret:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout repository
        uses: actions/checkout@v3

      - name: Show secret
        run: echo "Secret is ${{ secrets.TEST_SECRET }}"  # will be masked in logs
